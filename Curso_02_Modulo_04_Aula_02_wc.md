Curso 2, Módulo 4, Aula 2: Aprendi a contar linhas (`wc -l`) e palavras (`wc -w`). Usei o redirecionamento de entrada (`<`) para obter uma saída limpa (só o número) e armazenei o resultado em variáveis com `var=$(comando)`.

