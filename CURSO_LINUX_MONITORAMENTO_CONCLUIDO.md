Concluí o Curso 3: "Linux: criando script de monitoramento do sistema".
O projeto final foi um script modularizado (com funções) que monitora logs (grep/awk), rede (ping/curl), disco (df/du), hardware (free/top/iostat) e é automatizado com `systemd` (.service e .timer).
